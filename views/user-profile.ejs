<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link rel="stylesheet" href="/styles.css"> <!-- Include your styles if any -->
    <style>
        /* General Styles */
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f8f8f8; /* Light background color */
        }

        /* Navbar Styling */
        .top-bar {
            background-color: #333; /* Dark background for the navbar */
            color: #fff; /* White text */
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 10px 20px; /* Spacing around the navbar */
        }

        .top-bar .title-area h1 a {
            color: #e46e24; /* White text for the title */
            text-decoration: none; /* No underline */
            font-size: 1.8em; /* Font size for the title */
        }

        .top-bar .left {
            list-style: none; /* Remove bullet points */
            margin: 0;
            padding: 0;
            display: flex; /* Use flexbox for layout */
            align-items: center; /* Center items vertically */
        }

        .top-bar li {
            padding: 0 15px; /* Spacing between menu items */
        }

        .top-bar a {
            color: #fff; /* White text for links */
            text-decoration: none; /* No underline */
            font-size: 1.1em; /* Font size for links */
        }

        .top-bar a:hover {
            color: #ddd; /* Light gray on hover */
        }

        /* Profile Container */
        .profile-container {
            max-width: 900px; /* Max width for the profile container */
            margin: 40px auto; /* Center the container with some margin */
            padding: 20px; /* Padding inside the container */
            background-color: #fff; /* White background */
            border-radius: 10px; /* Rounded corners */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1); /* Subtle shadow */
        }

        .profile-container h2 {
            text-align: center; /* Center the heading */
            color: #333; /* Dark color for the heading */
            margin-bottom: 20px; /* Space below the heading */
            font-size: 2em; /* Font size for the heading */
        }

        .profile-container p {
            font-size: 1.1em; /* Font size for paragraph */
            color: #555; /* Slightly lighter color for text */
            margin: 10px 0; /* Space above and below paragraphs */
        }

        .edit-button {
            display: block; /* Block display to center it */
            margin: 20px auto; /* Center the button with margin */
            padding: 10px 20px; /* Padding for button */
            background-color: #e67e22; /* Button color */
            color: white; /* Button text color */
            border: none; /* No border */
            border-radius: 5px; /* Rounded corners */
            font-size: 1.1em; /* Font size */
            cursor: pointer; /* Pointer cursor on hover */
            text-align: center; /* Center text in button */
            width: fit-content; /* Fit to content */
        }

        .edit-button:hover {
            background-color: #d35400; /* Darker color on hover */
        }

        /* Quotes Section */
        .quotes-container {
            margin-top: 30px; /* Space above the quotes section */
            padding: 20px; /* Padding inside the quotes section */
            background-color: #f0f0f0; /* Light gray background for quotes */
            border-radius: 10px; /* Rounded corners */
        }

        .quote {
            font-style: italic; /* Italic style for quotes */
            color: #666; /* Gray color for text */
            margin-bottom: 15px; /* Space between quotes */
        }

        .quote-author {
            text-align: right; /* Align author text to the right */
            font-weight: bold; /* Bold author text */
            color: #333; /* Darker color for author */
        }

        /* Footer Styling */
        footer {
            background-color: #333; /* Dark background for the footer */
            color: white; /* White text */
            text-align: center; /* Center the text */
            padding: 20px 0; /* Padding around footer */
            position: relative; /* Relative positioning */
            bottom: 0; /* Stick to the bottom */
            width: 100%; /* Full width */
            font-size: 0.9em; /* Smaller font size for footer */
        }
    </style>
</head>
<body>
    <nav class="top-bar">
        <div class="title-area">
            <h1><a href="/">KickCraze</a></h1>
        </div>
        <ul class="left">
            <li><a href="/products">KickCraze</a></li>
            <li><a href="/cart">View Cart</a></li>
            <li><a href="/orders">My Orders</a></li>
            <li><a href="/contact">Contact</a></li>
            <% if (user) { %>
                <li><a href="/profile">My Account</a></li>
                <li><a href="/logout">Log Out</a></li>
            <% } else { %>
                <li><a href="/login">Log In</a></li>
                <li><a href="/register">Register</a></li>
            <% } %>
        </ul>
    </nav>

    <div class="profile-container">
        <h2>User Profile</h2>
        <p><strong>Full Name:</strong> <%= user.fullName %></p>
        <p><strong>Username:</strong> <%= user.username %></p>
        <p><strong>Email:</strong> <%= user.email %></p>
        
        <!-- Add more user information as needed -->
        <a href="/edit-profile?id=<%= user.id %>" class="edit-button">Edit Profile</a>



        
        <!-- Quotes Section -->
        <div class="quotes-container">
            <div class="quote">
                <p>"Empowering athletes, inspiring communities, one sneaker at a time."</p>
            </div>
                <div class="quote-author">- KickCraze</div>
            </div>
        </div>
    </div>

    <footer>
        <p>&copy; 2024 KicKCraze. All Rights Reserved.</p>
    </footer>
</body>
</html>
